[project]
name = "cm-transkriptionseq-scripts"
version = "0.1.0"
description = "Scripts for preprocessing chant transcription data"
dependencies = [
    "monodikit==0.0.10",
    "pandas==2.1.4"
]

[tool.setuptools]
py-modules = ["analyze_transcriptions", "logic", "test_normalize_folio"]

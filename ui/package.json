{
  "name": "ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:manual": "npm --prefix ../user-manual run build && rm -rf public/manual && cp -r ../user-manual/.vitepress/dist public/manual",
    "build:data": "(cd .. && python3 scripts/analyze_transcriptions.py)",
    "build:data:fresh": "rm -f ../transcription_cache.json && npm run build:data",
    "predev": "npm run build:manual && npm run build:data",
    "prebuild": "npm run build:manual && npm run build:data",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "canvg": "^4.0.3",
    "jspdf": "^4.0.0",
    "jspdf-autotable": "^5.0.7",
    "pinia": "^3.0.4",
    "vue": "^3.5.24",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.2.4"
  }
}

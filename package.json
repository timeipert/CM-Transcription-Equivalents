{
  "name": "cm-transkriptionseq",
  "private": true,
  "scripts": {
    "build:data": "python3 scripts/analyze_transcriptions.py",
    "build:data:fresh": "rm -f transcription_cache.json && npm run build:data",
    "build:manual": "npm --prefix user-manual run build && rm -rf ui/public/manual && cp -r user-manual/.vitepress/dist ui/public/manual",
    "build:app": "npm --prefix ui run build",
    "build": "npm run build:data && npm run build:manual && npm run build:app",
    "dev": "npm run build:data && npm run build:manual && npm --prefix ui run dev"
  }
}
